날짜별 코드수정사항



        2021-03-24
            1. 구글지도에서 -> 카카오맵으로 변경 (변경사유 : 한국인이 사용하기 너무 까다로움)

            2. 구글맵 api에서 여러가지 기능 추가
                현재 추가사항 : 마우스 올릴시 이벤트 이거 클릭으로 바꿀예정 3.25까지 예정
                              사용자 현재 실시간위치표시 현재는 주석처리해놓았지만 릴리즈시 주석해제예정
            3. jsp에서 db에 쿼리를 날려 위치정보Column인 PLACE와 위도Column인 LATITUDE와 경도Column인 LONGTITUDE 를 가져와서 좌표에 핀을찍음
                실력을 업해서 이걸 java에서 객체를 생성해 가져와보자

            4. 웹을 app으로 변경 이건 현재 css수정이 필요하다




            etc
               1. 메인메뉴 수정
               2. app으로 변경시 너무 허전함
               3. 여러가지 메뉴를 추가했으면 좋겠음
               4. 마우스클릭시 위치정보 표시기능 추가 (이건 얼마 걸리지 않을꺼같음)


            에러사항
                1. 에러코드 : invocation of init method failed nested exception is java.lang.illegalargumentexception
                    해결방안 : 13일 이걸로 애를 먹었다 이유는 내가 바보였다 DB연결시 model 부분에 @Getter, @Setter, @Entity(name = "테이블명")을 추가를 해줘야하지만 Getter,Setter는 추가했는데 바보같이 @Entity는 추가를 안해줬다

                2. 에러코드 :  Main class name has not been configured and it could not be resolved
                    해결방안 : 기존에 내가 package를 default로 주었는데 default로 주었을시 메인클래스에서 에러가난다 해결방법으로는 pakage를 default 에서 지정을 해주면된다
                             java소스 상단에 보면 pakage를써준다(ex: pakage com.example.myproject)

                3. 에러코드 :  Required String parameter (저장내용 DB업로드시 에러)
                    해결방안 : DB와 연동하는 controller안에 required값을 true에서 false로 바꿔서 해결 (하지만 이방법에 대해서는 조금더 공부를 해야할꺼같다, required=true는 파라메타를 필수적으로 받아야한다
                              false시 값이 없어도 저장이된다 게시판은 값이 들어와서 저장을 해야함으로 여기서는 true값이 들어가야한다)

                4. 에러코드 :  You have an error in your SQL syntax; check the manual that corresponds to your MariaDB server version for the right syntax to use near '(' at line 1 (실수중에 제일 ㅄ같은 실수였음)
                    해결방안 : repository에 들어가는 쿼리문중에 value = "INSERT INTO board (title, content) VALUES (?1, ?2)" 부분에서 ?2옆에 "," 이게들어가서 에러가 났다 ㅄ이다 눈뽑히는줄알았다......


                5. 에러코드 : query did not return a unique result (reposit과 controller에 쿼리오류)
                    해결방안 : 쿼리를 다시 찾아보고 쓴다

                6. 에러코드 : cannot parse data in timestamp string (데이터 타입오류)
                    해결방안 : 이런실수는 하지말자 DB에는 String으로 정했지만 model 에서 병신같이 date형으로 표기를 해줬다

                7. 에러코드 : Uncaught SyntaxError: missing ) after argument list (쿼리를 잘못보냄)
                    해결방안 : 처음에 select * from 테이블명 order by inx 로 날렸다 이건 데이터를 조회후 정렬하는 쿼리이다 결국 전부다 가져오는 코드이다
                              그래서 쿼리를 바꿔서 보냈다 select 컬럼,컬럼,컬럼 from 테이블;

                8. 에러코드 : is exceeding the 65535 bytes limit (65535bytes 를 초과했다 최대 jsp바이트수는 65535이다)
                    해결방안 : 멍청하게 하드코딩을 안하려고 db를 가져왔는데 하드코딩을 했다
                            {
                                content: '<%= rs.getString("PLACE") %>',
                                title: '<%= rs.getString("PLACE") %>',
                                latlng: new kakao.maps.LatLng(<%= rs.getString("LATITUDE") %>,<%= rs.getString("LONGTITUDE") %>)
                            }, 이걸 DB의 인덱스 수만큼 복사를 했다 이건 정말 바보같은 짓이였다

                9. 에러코드 : context with path [] threw exception [An exception occurred processing
                    해결방안 : connetion 을 확인해보자

